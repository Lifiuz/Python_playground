🤖 <b>Бот "Кто на смене?"</b>
Бот создан, чтобы решить проблему поиска сотрудников на смене и просмотра графика, чтобы избавить от постоянных обращений к сайту.

🛠 <b>Используемые технологии:</b>
• Python 3.11
• Библиотека <code>python-telegram-bot</code> 13.15
• Библиотека <code>Selenium</code> 4.33.0
• И другие необходимые импорты

📜 <b>Как работает бот:</b>
• Скрипт <code>STSK_schedule_parsing_script.py</code> парсит расписание с сайта (авторизацию преодолевает с учеткой gafurov.rb).
• График за каждый месяц сохраняется в соответствующий JSON в виде "словаря".
• Основной бот: <code>main_TGbot_body.py</code> показывает пользователю кнопки, отвечает за навигацию, вывод информации из JSON.
• Когда нужно показать, кто работает сегодня, берётся информация за текущее число у каждого инженера в графике текущего месяца.
• Когда нужно посмотреть, кто работал в определённую дату, предлагается выбрать месяц, день и выводится смена инженеров, кто работал (даже уволенных).
• При выборе "Смены инженера" предлагается выбрать инженера, который доступен на текущий месяц. Уволенных нельзя выбрать.
• Кнопка "Обратная связь" не анонимная и сохраняет для автора сообщения в блокнот.
• Парсер графика запускается вручную, так как график не часто меняется. Пока работает с локального ПК, не круглосуточно!

✅ <b>Сильные стороны:</b>
• Не зависит от количества дней в месяце.
• Не требует постоянного подключения к сайту.
• Безопасная авторизация.
• Обработка ситуаций, когда никого нет.

⚠️ <b>Уязвимые места:</b>
• Зависит от меток "ПРМ", "СПб", "МСК" в ФИО.
• Любой лишний пробел или его отсутствие нарушит сбор сотрудников.
• Ручной запуск и медленный парсинг.
• <code>credentials.json</code> хранит логин/пароль открыто.
• Данные разбиты по нескольким JSON-файлам.
• Перепроверяйте данные с ночниками.
• Не собрал ссылки на профиль всех инженеров.

🚀 <b>Возможности роста:</b>
• Версия 1 – показывает кто на смене [Реализовано]
• Версия 2 – добавлены кнопки "Кто работал" и "Смены инженера" [Реализовано]
• Версия 3 – добавлена кнопка обратной связи и пароль при входе в бот [Реализовано]
• Выводить отпуска, вести учёт накопленных дней
• Выводить график и контакты руководителей
• Выводить информацию, доступную для копирования (моноширинным шрифтом) или для экспорта
• Выводить информацию по другим отделам (B20, B2B...)
• Считать количество отработанных часов и смен

📊 <b>Алгоритм определения смены:</b>
1. Определяется текущая дата и час
2. В <code>XX_2025.json</code> проверяется значение по текущему дню, где XX - номер текущего месяца
3. Если это число → инженер в смене
4. Корректировка по городу: НСК +2 ч, СПб/РНД/МСК -2 ч от Пермского
5. Сравниваем с текущим часом — если ещё в смене, выводим

Длительность смены берётся из <code>work_hour.json</code> (9 или 12 ч)
Отдел инженера берётся из <code>departament.json</code>.
Ссылка на профиль телеграма берётся из <code>telegram_link.json</code>.
Информация о боте выводится из <code>about.txt</code>.
Учётные данные для авторизации на kb берутся из <code>credentials.json</code>.

🧪 Файл <code>debugging.txt</code> показывает результат парсинга таблицы.

Для связи с автором используется кнопка "Обратная связь".
